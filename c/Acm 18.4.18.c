#include<stdio.h>
#include<string.h>
int main()
{
    char s[2000];
    char t[2000];
    scanf("%s", s);
    scanf("%s", t);
    int 11=strlen(s);
    int 12=strlen(t;
                  int i,j;
                  int cc=0;
                  for(i=0; i<11; i++)
{
    if( s[i]==t[o])
        {
            int f=0;
            for(j=1; j<12; j++)
            {
                if(s[i+j]!=t[j])
                {
                    f=1;
                }
            }
            if(f==0)
            {
                c++;
            }

        }


    }
}
