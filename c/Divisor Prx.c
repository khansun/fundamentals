#include<stdio.h>
#include<math.h>
//use the prime finder function written on the notebook
    int main(){
        int num,i;
        scanf("%d", &num);
            int NOD(int N)
            {
               // if (status[N]==0)
                  //  return 2;
            //}
            int sqr=sqrt(N);
            for(i=0; prime[i]<=sqr;i++)
                {
                    if(N==1)
                        break;
                i(N%prime[i]==0)
                {
                    int cc=0;
                }

            while(N%prime[i]==0)
            {
                N/=prime[i];
                cc++;
            }
            num*=(cc+1);
        }
        return num;
    }
